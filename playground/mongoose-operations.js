/*
var newTodo = new Todo({
    text : '  a   '
});

newTodo.save().then((res) => {
    console.log('Saved todo succesfully',JSON.stringify(res,undefined,2));
},(err) => {
    console.log('unable to add todo',err);
});


var otherTodo = new Todo({
    text : 'lunch',
    completed : 'true',
    completedAt : '1856'
});

otherTodo.save().then((res) => {
    console.log('Saved todo succesfully',res);
},(err)=>{
    console.log('unable to add todo',err);
});   */